[package]
name = "ironclaw_trusted_ingress"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"
description = "Host-only trusted ingress authority tokens for IronClaw Reborn"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[dependencies]

[features]
default = []
composition-root = []
