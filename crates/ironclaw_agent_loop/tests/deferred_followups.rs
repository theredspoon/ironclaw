#[tokio::test]
#[ignore = "WS13 adds the host cancellation accessor; enable this as the negative-to-positive integration test then"]
async fn cancellation_accessor_short_circuits_loop_when_ws13_lands() {
    panic!(
        "WS13 must replace this placeholder with an executor-level cancellation integration test"
    );
}
