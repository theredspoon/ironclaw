[package]
name = "ironclaw_reborn"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "Standalone Reborn composition and adapters for IronClaw"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[features]
default = []
root-llm-provider = ["dep:ironclaw_llm", "dep:base64"]
# Bare libSQL dependency toggle, mirroring the root `ironclaw` crate's `libsql`
# feature. CI lints/builds the whole workspace with `--all --features libsql`
# (e.g. code_style's `libsql-only` leg and platform-and-compat); without this,
# feature resolution fails because `ironclaw_reborn` is the lone workspace
# member that enables libSQL only through the more specific features below.
libsql = ["dep:libsql"]
libsql-secrets = [
    "dep:ironclaw_secrets",
    "dep:libsql",
    "dep:secrecy",
    "dep:ironclaw_filesystem",
    "ironclaw_filesystem/libsql",
    "filesystem-goal-store",
]
filesystem-goal-store = ["dep:ironclaw_filesystem"]
# Local-dev trigger-fire access store (libSQL-backed). Named for its original
# WebChat user-store home; the canonical user store has since moved to the
# filesystem identity resolver, so this feature now only gates the
# `local_trigger_access` module.
webui-user-store = ["dep:libsql"]
filesystem-local-trigger-access = ["dep:ironclaw_filesystem"]
# Opt-in because the restart coverage opens real libSQL-backed turn/thread stores.
# Keep separate from `libsql-secrets` so local `cargo test -p ironclaw_reborn`
# stays fast, while CI can exercise process-restart persistence explicitly.
libsql-restart-tests = [
    "dep:libsql",
    "dep:ironclaw_filesystem",
    "ironclaw_filesystem/libsql",
    "filesystem-goal-store",
]

[dependencies]
async-trait = "0.1"
base64 = { version = "0.22", optional = true }
chrono = "0.4"
futures-util = { version = "0.3", default-features = false }
ironclaw_agent_loop = { path = "../ironclaw_agent_loop", version = "0.1.0" }
ironclaw_events = { path = "../ironclaw_events", version = "0.1.0" }
ironclaw_hooks = { path = "../ironclaw_hooks", version = "0.1.0" }
ironclaw_host_api = { path = "../ironclaw_host_api", version = "0.1.0" }
ironclaw_host_runtime = { path = "../ironclaw_host_runtime", version = "0.1.0" }
ironclaw_llm = { path = "../ironclaw_llm", version = "0.1.0", optional = true, default-features = false }
ironclaw_loop_support = { path = "../ironclaw_loop_support", version = "0.1.0" }
ironclaw_observability = { path = "../ironclaw_observability" }
ironclaw_safety = { path = "../ironclaw_safety", version = "0.2.2" }
ironclaw_secrets = { path = "../ironclaw_secrets", version = "0.1.0", optional = true }
ironclaw_filesystem = { path = "../ironclaw_filesystem", version = "0.1.0", optional = true }
ironclaw_threads = { path = "../ironclaw_threads", version = "0.1.0" }
ironclaw_turns = { path = "../ironclaw_turns", version = "0.1.0" }
libsql = { version = "0.9", optional = true, default-features = false, features = ["core", "replication", "remote", "tls"] }
parking_lot = "0.12"
secrecy = { version = "0.10", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }
tokio-util = { version = "0.7", features = ["rt"] }
tracing = "0.1"

[dev-dependencies]
chrono = "0.4"
ironclaw_agent_loop = { path = "../ironclaw_agent_loop", version = "0.1.0", features = ["test-support"] }
ironclaw_authorization = { path = "../ironclaw_authorization", version = "0.1.0" }
ironclaw_event_projections = { path = "../ironclaw_event_projections", version = "0.1.0" }
ironclaw_extensions = { path = "../ironclaw_extensions", version = "0.1.0" }
ironclaw_filesystem = { path = "../ironclaw_filesystem", version = "0.1.0" }
ironclaw_processes = { path = "../ironclaw_processes", version = "0.1.0" }
ironclaw_reborn_event_store = { path = "../ironclaw_reborn_event_store", version = "0.1.0" }
ironclaw_resources = { path = "../ironclaw_resources", version = "0.1.0" }
ironclaw_scripts = { path = "../ironclaw_scripts", version = "0.1.0" }
ironclaw_skills = { path = "../ironclaw_skills", version = "0.3.0", default-features = false }
ironclaw_trust = { path = "../ironclaw_trust", version = "0.1.0" }
rust_decimal = "1"
serde = "1"
serde_json = "1"
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
wat = "1.245.1"
tracing-test = { version = "0.2", features = ["no-env-filter"] }
tracing-subscriber = "0.3"

[[test]]
name = "llm_gateway"
required-features = ["root-llm-provider"]

[lints]
workspace = true
