[package]
name = "ironclaw_wasm_product_adapters"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "WASM v2 host glue for IronClaw Reborn ProductAdapters (#3285)"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[package.metadata.ironclaw]
layer = "products"

[dependencies]
async-trait = "0.1"
chrono = "0.4"
hmac = "0.13"
hex = "0.4"
http = "1"
ironclaw_product_adapters = { path = "../ironclaw_product_adapters", version = "0.1.0", features = ["host-auth-mint"] }
ironclaw_wasm = { path = "../ironclaw_wasm", version = "0.1.0" }
serde_json = "1"
sha2 = "0.11"
subtle = "2"
thiserror = "2"
tokio = { version = "1", features = ["rt", "sync", "time"] }
tracing = "0.1"
wasmtime = { workspace = true }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "sync"] }
wat = "1.245.1"
wit-component = "0.245.1"
wit-parser = "0.245.1"
