# ascii-renderer.draw

Renders a small piece of canned ASCII art and returns it. Optional `subject`
selects the drawing: `cat`, `dog`, or `robot` (defaults to `robot`). An unknown
subject falls back to the robot.

Use this whenever the user asks for ASCII art or a quick little drawing. The
tool needs no network access and no credentials — it is pure compute.
